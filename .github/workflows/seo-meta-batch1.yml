name: SEO Meta Batch 1 Smoke Test

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/seo-meta-batch1.yml'

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test
        shell: bash
        run: |
          echo "SMOKE_OK"
          echo "GitHub Actions runner is working"