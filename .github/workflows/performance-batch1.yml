name: Performance Batch 1

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/performance-batch1.yml'

permissions:
  contents: write

jobs:
  performance-batch1:
    runs-on: ubuntu-latest