# Generated article-quality reports (keep the directory via .gitkeep)
reports/article-quality/*.json
reports/article-quality/*.html
# OS noise
.DS_Store
Thumbs.db
# Generated batch manifests are regenerable; batch REPORTS are committed
# (reports/batches/BATCH-XXX.json|md) because they are the durable audit
# trail of each production run.
data/batches/*.json
data/batches/*.lock
